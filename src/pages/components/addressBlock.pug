mixin addressBlock(label="", selected= false) 
  .addressBlock
    .addressBlockLeft
      .addressBlockLabel= label
      if selected
        .addressSelected
          img.addressSelectedIcon(src=require('images/Selected.svg'), alt="_img")
          .addressSelectedText Выбранный адрес
    .addressBlockRight
      img(src=require('images/Edit.svg'), alt="_img")