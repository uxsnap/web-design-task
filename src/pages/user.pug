doctype html
html(lang="en")
  head
    title User profile
    meta(charset="UTF-8")
    link(href=require("styles/user.scss") rel="stylesheet")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
  body.user
    include components/header.pug
    +header(false, "disableUser")

    .main
      include components/userHeader.pug
      include components/userData.pug
      include components/userAddresses.pug
      include components/orderHistory.pug
      script(src=require("scripts/user.js"))